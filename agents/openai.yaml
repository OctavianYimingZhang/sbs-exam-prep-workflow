interface:
  display_name: "SBS Exam Prep Workflow"
  short_description: "Evidence-grounded exam-prep workflow with setup, planning, Excel, and optional Example Essay DOCX routes"
  default_prompt: "Use $sbs-exam-prep-workflow to turn my lecture slides, official notes, past papers, practical materials, answer keys, exemplars, feedback, and readings into the narrowest valid exam-prep route. Build a SkillConfig, create a WorkflowPlan, check input readiness, then generate the requested student-facing output while keeping helper artifacts separate unless I ask for an audit package."

policy:
  allow_implicit_invocation: true
